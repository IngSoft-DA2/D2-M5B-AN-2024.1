import { CharactersFilterPipe } from './characters-filter.pipe';

describe('CharactersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CharactersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
